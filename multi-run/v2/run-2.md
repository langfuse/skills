# Langfuse Issue-Detection Triage — Run #2

**Project:** `clkpwwm0m000gmm094odg11gi` (EU cloud, `https://cloud.langfuse.com`)
**Generated:** 2026-07-08 (UTC)
**Trace link base:** `https://cloud.langfuse.com/project/clkpwwm0m000gmm094odg11gi/traces/<traceId>`

---

## Overall stats

### Window & scope
- **Window analyzed:** `2026-07-01T17:42:08Z` → `2026-07-08T17:42:08Z` (last 7 days, UTC).
- **Product environment chosen:** `default`.
- **Excluded (machinery):** `langfuse-llm-as-a-judge` = 1,556 evaluator executions; `sdk-experiment` = 170 dataset/experiment runs. These are the eval/offline harness, not product traffic.
- **Raw window total:** 2,062 traces (1,556 judge + 336 product + 170 experiment).

### Volume examined (full pulls, no sampling cap hit)
- **336 product traces** (`core,io,metrics`) — paged in full.
- **2,635 observations** in `default` (`core,basic,usage,model,metrics`); **485 TOOL** observations re-pulled with `io` for content inspection.
- **1,370 scores** over the window; **1,200** attach to product traces.

### Product mix (by trace name, environment=default)
| Name | Traces |
|---|---|
| QA-Chatbot (main conversational product) | 263 |
| Image-Generator | 42 |
| Sentiment-Classifier | 21 |
| livekit-voice-agent | 9 |
| claude-code-test-trace | 1 |

### Input distribution (profiled BEFORE any rate — all rates below are conditioned on this)
QA-Chatbot inputs are dominated by canned/demo traffic: **`"What can I use Langfuse for?"` appears 102 / 263 times (39%)**; another ~14% are greetings ("hi/hello/你好") and garbage tokens ("123", "as", "hgi", "发呆"). Distinct inputs: 129/263. **This is almost certainly synthetic / landing-page / demo traffic and it skews every aggregate** (latency, cost, and evaluator distributions are pulled toward this one question). It is not tagged or routed to a separate environment. All numbers below are stated against the 263 QA traces as examined; the real long-tail is under-measured.

### Headline metrics
| Metric | Value |
|---|---|
| Product traces examined | 336 (QA-Chatbot 263) |
| QA-Chatbot latency (end-to-end, root) | p50 **20.5s**, p90 40.8s, p99 **103.3s**, max 156.2s |
| Image-Generator latency | p50 12.9s, p99 20.8s |
| Sentiment-Classifier latency | p50 2.9s |
| Total cost (window, product) | **$9.50** (QA mean $0.034/trace, max $0.166) |
| Models in use | `gpt-5-2025-08-07` (QA, 740 gen calls), `gpt-image-1` (img), `gpt-4o-mini` (sentiment), `gpt-4.1-mini`/`FallbackAdapter` (voice) |
| Hard errors (level=ERROR) | 5 observations (all `searchLangfuseDocs`, HTTP 504) |
| Silent errors (in output body, DEFAULT level) | +13 `getLangfuseDocsPage` "Failed to fetch" |
| Empty user-facing outputs (QA) | 2 / 263 (0.76%) |
| User feedback scores in window | **1** (positive) |

---

## Findings (ranked)

| # | Issue | Category | Priority | Traces affected | Description | Root-cause hypothesis | Example traces | Proposed fix | Agent prompt |
|---|---|---|---|---|---|---|---|---|---|
| 1 | Response-grading evaluators mis-wired (blind quality signal) | Scores/Evals | **P0** | ≥ 373 scores across 263 QA traces | The two evaluators that grade the assistant's *response* — **"Language & style"** (NUMERIC) and **"User disagreement customer support"** (BOOLEAN) — never receive the assistant output. "Language & style" is 91% `0` (239 of 263) and **75% of its comments literally say** "there is no prior assistant message… only a system prompt". "User disagreement" is 109/110 `False` and **82% of comments say** "no prior assistant response… score False". Every value they produced is noise. The only response-*correctness* evaluator ("Correctness") runs **offline on the sdk-experiment dataset (169 runs), not on production** — so in production **no valid evaluator measures response quality at all.** The product could silently degrade and nothing would show it. | Evaluator variable mapping omits the trace `output` / assistant turn; the judge template's `CONVERSATION_HISTORY` / generation variable is bound to the system prompt + user message only. Diagnosed from reading comments, not values — this is a wiring bug, NOT miscalibration; do not "recalibrate". | [Language&style zeros: trace 736d251fa4b277633b2bedd9dc244887](https://cloud.langfuse.com/project/clkpwwm0m000gmm094odg11gi/traces/736d251fa4b277633b2bedd9dc244887) · [1fb27ea7a8c14caec44a06e66a19c80d](https://cloud.langfuse.com/project/clkpwwm0m000gmm094odg11gi/traces/1fb27ea7a8c14caec44a06e66a19c80d) | In the Langfuse evaluator config for "Language & style" and "User disagreement customer support", fix the variable mapping so the assistant/trace `output` (final response) is passed into the judge prompt. Re-run on a backfill window to get real values, then set alerts on the negative direction. | The Langfuse LLM-as-a-judge evaluators "Language & style" and "User disagreement customer support" are producing garbage: 91% of Language & style scores are 0 and 82% of disagreement scores are False, and their own reasoning comments say "there is no prior assistant message in the CONVERSATION_HISTORY; only a system prompt". The evaluators are grading only the system prompt + user message and never see the assistant's actual reply. Fix the evaluator variable mapping for both so the trace `output` (the QA-Chatbot's final assistant message) is injected into the judge template variable that represents the assistant response. Confirm by checking that new score comments reference the actual answer text. Note that "Correctness" runs only on the offline experiment dataset, so production response quality is currently unmonitored — after fixing, wire at least one response-quality evaluator to run on the `default` production environment. |
| 2 | Slow interactive chatbot | Latency | **P1** | 263 / 263 QA | QA-Chatbot end-to-end **p50 = 20.5s**, p90 = 40.8s, **p99 = 103.3s**, max 156.2s. For an interactive doc chatbot this is felt on essentially every turn (high p50, not just the tail). Dominated by the main LLM: `ai.streamText` (gpt-5) median 20.1s / max 156s; `searchLangfuseDocs` adds median 5.3s (max 60s). | `gpt-5-2025-08-07` streaming latency is the bottleneck; sequential tool round-trips (search → fetch page → stream) compound it. Model choice + serial tool calls, not a bug. | [chat_c2bbf548 turns](https://cloud.langfuse.com/project/clkpwwm0m000gmm094odg11gi/traces/94ba2e40b9) (representative slow turns) | Stream tokens to the user immediately (TTFT), parallelize retrieval tool calls, consider a faster model for the first pass or for greeting/short-answer turns; cap `searchLangfuseDocs` timeout. | The QA-Chatbot (model gpt-5-2025-08-07, AI SDK `streamText`) has p50 20.5s and p99 103s end-to-end latency, felt on every turn. Investigate: (a) is time-to-first-token being surfaced to the user or is the UI blocking on full completion? (b) are `searchLangfuseDocs`/`getLangfuseDocsPage` tool calls serial when they could be parallel? Reduce perceived latency by streaming early and parallelizing retrieval; evaluate a lower-latency model for short/greeting turns. |
| 3 | Silent RAG tool failures → empty answers | Reliability | **P1** | 11 / 263 tool-error traces; 2 empty outputs | `getLangfuseDocsPage` fails **13 / 97 calls (13.4%)** with `"Error fetching docs page markdown: Failed to fetch https://langfuse.com/..."` — all emitted at **DEFAULT level (invisible to error monitoring)**. `searchLangfuseDocs` returns HTTP 504 on 5 calls (these DO surface at ERROR). Together they hit 11 traces; **2 of them produced a completely empty user-facing answer** (`736d251…` "What can I use Langfuse for?", `1fb27ea7…` "What is Prompt Management…"). The other 9 recovered from partial context. The silent (DEFAULT-level) share is the real hazard — 13 failures that dashboards would miss. | Upstream docs/RAG backend (inkeep + langfuse.com markdown fetch) is intermittently unavailable (504 / fetch failure); the tool swallows the failure into a normal-level output body, and when it's the only context source the agent emits nothing. | [empty: 736d251fa4b277633b2bedd9dc244887](https://cloud.langfuse.com/project/clkpwwm0m000gmm094odg11gi/traces/736d251fa4b277633b2bedd9dc244887) · [504: 1fb27ea7a8c14caec44a06e66a19c80d](https://cloud.langfuse.com/project/clkpwwm0m000gmm094odg11gi/traces/1fb27ea7a8c14caec44a06e66a19c80d) · [d40f57de32](https://cloud.langfuse.com/project/clkpwwm0m000gmm094odg11gi/traces/d40f57de3296e5827ffd553286e60797) | Emit tool fetch/HTTP failures at ERROR level (so they're monitored); add retry-with-backoff on `getLangfuseDocsPage`/`searchLangfuseDocs`; guarantee a graceful fallback answer instead of an empty string when all retrieval fails. | The RAG tools `getLangfuseDocsPage` and `searchLangfuseDocs` fail silently: `getLangfuseDocsPage` returns `"Error fetching docs page markdown: Failed to fetch …"` on ~13% of calls at DEFAULT log level, and `searchLangfuseDocs` throws HTTP 504. When these are the only context source the QA-Chatbot returns an empty string to the user (see traces 736d251fa4b277633b2bedd9dc244887 and 1fb27ea7a8c14caec44a06e66a19c80d). Fix: (1) set observation level to ERROR on any non-2xx / fetch failure so it's alertable; (2) add retry+timeout on both tools; (3) never return an empty assistant message — fall back to a helpful "I couldn't fetch the docs, here's what I know / please retry" response. |
| 4 | Wrong evaluator applied to wrong products | Scores/Evals | **P2** | 63 / 336 (img + sentiment) | The `user-intent` evaluator runs on **all** product trace types, including Image-Generator (42) and Sentiment-Classifier (21), classifying their inputs ("A sunset over mountains", "Hey classify this: …") as `irrelevant-to-langfuse`. This inflates the global "irrelevant" count (140/335) and spends eval $ grading traces the evaluator was never designed for. On QA-Chatbot alone, irrelevant-to-langfuse is 68/263 (26%). | Evaluator target/filter is set to the whole project/environment instead of being scoped to `name = QA-Chatbot`. | [Image-Generator trace 8f6449bd6ff17d7aa86af291ed8b3454](https://cloud.langfuse.com/project/clkpwwm0m000gmm094odg11gi/traces/8f6449bd6ff17d7aa86af291ed8b3454) | Scope each evaluator's target filter to the trace name(s) it's meant for (QA-Chatbot), excluding Image-Generator / Sentiment-Classifier / voice. | The `user-intent` (and other QA-oriented) evaluators are firing on Image-Generator and Sentiment-Classifier traces and marking them "irrelevant-to-langfuse". In the evaluator target configuration, restrict the run filter to `trace.name = "QA-Chatbot"` (and the QA environment) so image/sentiment traces aren't graded by chatbot evaluators. |
| 5 | Inconsistent out-of-scope handling + language drift | Output quality/Safety | **P2** | ≥ 1 fully-served off-topic (of ~18 out-of-scope) | Out-of-scope requests (world-cup scorer, weather, "发呆") are **mostly declined gracefully** with a Langfuse redirect — good. **But at least one off-topic request was fully served**: input was an `open-meteo`/pandas weather-API Python snippet and the agent **wrote the full weather-API integration code, and answered in Portuguese** ("Para Macaíba-RN, basta ajustar a latitude/longitude…") despite no Portuguese in the input. So handling is inconsistent, and there's a language-selection bug. | Scope guard depends on the LLM's judgment per-turn; a code-shaped input bypasses the "I only do Langfuse" guard, and locale inference picks a wrong language from place-name tokens. | [28b2a2193e (off-topic world-cup, handled well)](https://cloud.langfuse.com/project/clkpwwm0m000gmm094odg11gi/traces/28b2a2193e) — plus the open-meteo/Portuguese case in the QA set | Strengthen the system-prompt scope guard for code inputs; always answer in the user's input language (or a configured default), never infer from geographic tokens. | The QA-Chatbot mostly declines off-topic questions but sometimes fully answers them — e.g. it wrote a complete open-meteo weather-API integration for a user, and replied in Portuguese even though the input wasn't Portuguese. Tighten the system prompt so code-shaped off-topic requests are also politely redirected to Langfuse topics, and fix language selection so the reply language matches the user's message (don't infer language from place names like "Macaíba-RN"). |
| 6 | Multi-turn user friction / unresolved struggle | User friction | **P2** | 1 session (28 turns) + smaller repeats | Session `chat_c2bbf548…` ran **28 turns over ~2 hours** with the user repeatedly re-asking the same thing (setting a custom trace name, `propagate_attributes`, "Fast Preview v4"), with explicit failure signals: "No root is not getting the trace name idk why did as u said", "What is xrong in my code". The agent kept re-explaining without resolving. High-effort, unresolved. | First answers were underspecified / didn't match the user's actual v4 setup; possible faithfulness risk in the "Fast Preview v4" explanations (couldn't verify against ground truth). | [chat_c2bbf548 (28-turn session)](https://cloud.langfuse.com/project/clkpwwm0m000gmm094odg11gi/traces/94ba2e40b9) | Detect repeated re-asks in a session and switch strategy (ask a clarifying question, request the user's actual code/screenshot); verify "Fast Preview v4" claims against current docs. | Review session chat_c2bbf548-517c-42ea-83b7-093b9a479f54: a user spent 28 turns unable to get a custom trace name to show in Langfuse v4 "Fast Preview", repeatedly saying it didn't work. The agent re-explained `propagate_attributes` many times without resolving. Add a clarification/step-back behavior when the same intent repeats 3+ times in a session, and verify that the agent's claims about Langfuse v4 "Fast Preview" trace-naming are factually correct against the current docs (possible hallucination). |
| 7 | Synthetic/demo traffic not segregated | Input/Scope | **P2** | 102 / 263 QA (39%) | One canned question `"What can I use Langfuse for?"` is 39% of QA traffic; plus ~14% greetings/garbage. It's mixed into the `default` production environment, so it drags every aggregate (latency/cost/eval distributions) toward the canned case and hides the real long tail. | Landing-page demo / CI / synthetic load is emitted to the same environment and tag as real users. | [102× canned: e.g. 87fab7962c…](https://cloud.langfuse.com/project/clkpwwm0m000gmm094odg11gi/traces/87fab7962c) | Route demo/CI traffic to a separate `environment` (e.g. `demo`) or tag it, and exclude from production dashboards/evals. | Traffic to the QA-Chatbot is 39% a single canned question ("What can I use Langfuse for?") plus many greetings/garbage inputs, all in the `default` environment mixed with real users. Tag or route this synthetic/demo/landing-page traffic to a separate environment so production metrics and evaluator distributions reflect real usage. |
| 8 | Monitoring coverage gaps | Coverage | **P3** | see cells | (a) **Only 1 user-feedback score** in the entire 7-day window — near-zero ground-truth satisfaction signal (F16 blind spot). (b) `out_of_scope` evaluator ran on only **228 / 263** QA traces (35 unscored). (c) `is_cursing` is a **constant `False` (263/263)** — reads the user message (correctly wired) but never fired; legitimately quiet, needs a known-positive sanity test. (d) Image-Generator (42) and livekit-voice-agent (9) have **null trace-level `output`** — the image is captured at the observation level as `@@@langfuseMedia@@@` and voice output is on `llm_node`, so the products work, but trace.output isn't populated. Voice at 9 traces is too low to certify. | Feedback capture not wired to UI; evaluator sampling < 100%; trace-output not set on media/voice pipelines. | [voice: 7c2265b8c8f9893fb96d149c083d743a](https://cloud.langfuse.com/project/clkpwwm0m000gmm094odg11gi/traces/7c2265b8c8f9893fb96d149c083d743a) | Wire thumbs-up/down capture; run evals on 100% (or a fixed sample) of QA; add a known-positive test for `is_cursing`; set trace.output on image/voice pipelines. | Improve monitoring coverage: (1) only 1 user-feedback score exists in 7 days — verify the thumbs-up/down capture is wired end-to-end; (2) the `out_of_scope` evaluator only scored 228 of 263 QA traces — check the sampling/target filter; (3) `is_cursing` is constant False — add a known-profanity test input to confirm it can fire; (4) Image-Generator and voice-agent traces have empty trace-level output — set trace.output so the top-level trace shows the result. |

---

## Verified non-issues (checked and clean)

- **Truncation (A4):** no generations hit `finish_reason: length` / max_tokens cut-offs in the sampled generations.
- **Cost explosions (C9):** max single QA trace $0.166; total window $9.50. No runaway-token trace; per-trace cost distribution is tight.
- **Tool loops / retry storms (D12):** no trace showed a tool called in a pathological loop; tool counts per trace are sane (search → fetch → stream).
- **Image-Generator quality (A3):** the 42 null *trace* outputs are an instrumentation detail — images ARE produced and captured at the observation level (`gpt-image-1`, `@@@langfuseMedia@@@`). Not a product failure.
- **Cursing / toxicity (E, safety):** `is_cursing` = 0 True across 263; spot-read of inputs confirms no profanity in the window (legitimately quiet, see coverage note 8c).
- **Valid evaluators:** `user-intent`, `out_of_scope`, `is_cursing` read the user message correctly (0% "missing-assistant" comments) and produce sensible distributions — they are trustworthy for the *user-input* dimension (unlike the response-graders in Finding 1).
- **PII / secret leakage (G27):** no secrets/PII or successful prompt-injection observed in the sampled inputs/outputs.
- **Language matching (G30):** aside from the one Portuguese case (Finding 5), the agent correctly mirrors Chinese/English input language.

## Suggested order of action

1. **Finding 1 (P0)** — fix the two response-grader evaluator mappings and wire a valid response-quality evaluator to production; you are currently blind to answer quality.
2. **Finding 3 (P1)** — surface tool failures at ERROR level + retry + no-empty-output fallback (quick, stops silent user-facing empties).
3. **Finding 2 (P1)** — attack QA latency (stream TTFT, parallelize retrieval, model choice).
4. **Findings 4–7 (P2)** — scope evaluators to correct products, tighten off-topic/language handling, segregate demo traffic, address multi-turn friction.
5. **Finding 8 (P3)** — close monitoring/coverage gaps.

## Open questions

- Is `"What can I use Langfuse for?"` (102×) genuine landing-page traffic or a load test? Confirm so it can be routed out of production metrics.
- Is "Fast Preview v4" trace-naming guidance (session `chat_c2bbf548`) factually correct, or is the agent hallucinating an API? Needs a docs cross-check.
- Should `Correctness` (currently offline-only on the experiment dataset) also run on production QA traffic?

## Not covered / limitations

- **Faithfulness/hallucination (G26)** not measured quantitatively — no ground truth for the doc answers; flagged only where a session suggested it (Finding 6).
- **livekit-voice-agent** at 9 traces is below the volume needed to certify health; treated as insufficient data, not "clean".
- Response-quality distributions could not be computed for the window because the only response-grading evaluators are mis-wired (Finding 1) — that dimension is unmeasured until they're fixed.
- Retrieval *relevance* (D14) was checked for gross failures (errors, empty sets) but not scored per-query for semantic match against ground truth.
