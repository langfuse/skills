# Langfuse Issue-Detection Triage — Run #4

**Project:** `cmfzimu6801jbad079856b07r` (Langfuse in-app-agent) · EU cloud (`https://cloud.langfuse.com`)
**Generated:** 2026-07-08 (independent run #4 of 5)

---

## Overall stats

### Scope
- **Window analyzed:** `2026-07-01T19:08:22Z → 2026-07-08T19:08:22Z` (last 7 days, UTC).
- **Environments included (all real product traffic):**
  - `langfuse-in-app-agent` — 221 traces. Surfaces: `agent-turn` (79, the core conversational agent), `conversation-title` (117, background title generation), `in-app-agent` (25, conversation-root spans).
  - `langfuse-natural-language-filter` — 33 traces. Surfaces: `search-bar-filter` (32) + `natural-language-filter` (1) — NL→filter-JSON generation for the search bar.
- **Excluded:** **None.** There were **no** `langfuse-llm-as-a-judge` evaluator executions, dataset runs, or playground traces in the window. The complete absence of evaluator machinery is itself a coverage finding (see F1).

### Volume examined
- **254 traces** (100% of the window — pulled `core,io,metrics`, all 3 pages).
- **865 observations** (330 TOOL, 281 GENERATION, 254 SPAN — pulled `core,basic,usage,model,metrics`, then a full `io` re-pull of all 330 TOOL observations for content inspection).
- **2 scores** (the entire window).

### Headline metrics

| Surface | n | Latency p50 / p90 / p99 / max | Cost (window) | Model |
|---|---|---|---|---|
| `agent-turn` (core agent) | 79 | **13.9s / 55.1s / 82.2s / 100.0s** | **not instrumented ($0 / null tokens)** | not reported |
| `conversation-title` | 117 | 1.0s / 1.7s / 3.4s / 4.3s | $0.18 | Haiku-4.5 (102) + Opus-4.8 (15) |
| `search-bar-filter` | 32 | 2.1s / 4.1s / 7.3s / 7.3s | **$1.32** (mean $0.041) | Opus-4.8 |
| `in-app-agent` (conv. root) | 25 | 29s / 2667s / 5943s / 5943s (session duration, not per-turn) | $0 / null | — |

- **Error rate:** 6 observations at `level=ERROR`; **5 malformed-tool-arg failures** across 3 turns + **1 context-overflow hard failure** on an agent-turn; **13 empty tool outputs** (silent). Content-level scan (not just `level`) confirmed no additional swallowed failures beyond these (the 504/exception substrings in tool outputs were inside *returned data*, not tool status).
- **Scored traffic:** 2 / 254 = **0.8%**. Both scores are `in_app_agent_feedback = true` (positive user thumbs-up; one comment: *"nice worked but was slow"*). **Zero negative user signals. Zero LLM-as-a-judge evaluators running on production.**
- **Input distribution (trustworthiness check):** NOT synthetic-skewed. 65 distinct inputs across 79 agent-turns, 41 distinct users (top user 33 traces). Most-repeated input is 4/79 (5%). All rates below are on genuine diverse power-user traffic; no canned-input dominance to correct for.

---

## Findings

| # | Issue | Category | Priority | Traces affected | Description | Root-cause hypothesis | Example traces | Proposed fix | Agent prompt |
|---|---|---|---|---|---|---|---|---|---|
| 1 | No production quality evals | Scores/Evals | **P1** | 252 / 254 unscored | Only 2 scores exist in 7 days (0.8% coverage), both manual positive thumbs-up. **No LLM-as-a-judge evaluator runs at all** on the core `agent-turn` surface. The product's answer quality (correctness, helpfulness, faithfulness) is entirely unmeasured — a degradation would be invisible. | No evaluator is wired to the `langfuse-in-app-agent` environment / `agent-turn` traces; the team relies on sparse manual feedback only. | https://cloud.langfuse.com/project/cmfzimu6801jbad079856b07r/traces/arun_so61aeywm94leindtfsirou6-trace | Stand up at least one LLM-judge evaluator (helpfulness + task-completion) targeting `environment=langfuse-in-app-agent`, `name=agent-turn`, mapped to the trace `output` assistant turn. Backfill on the last 7 days. | Set up a Langfuse LLM-as-a-judge evaluator on the `langfuse-in-app-agent` project scoped to traces named `agent-turn`. It must read the trace `input.messages` (user question) and the final assistant message in `output.messages` and score task-completion + helpfulness (0/1 with reasoning comment). Verify the variable mapping actually captures the assistant output (read 3 comments after first runs to confirm it is not grading an empty response). Currently 0 evaluators run in production. |
| 2 | Core agent cost/tokens un-instrumented | Cost | **P1** | 131 / 131 core generations | `agent-turn` and `agent-run` GENERATIONs report `totalCost=0`, `inputTokens/outputTokens=0/null`, and empty `model`. The main product's LLM spend and token growth are invisible; only peripheral features (`conversation-title`, `search-bar-filter`) report cost. Cost/context monitoring on the primary surface is blind. | The agent's LLM calls (likely Bedrock via a streaming wrapper) are not passing usage/model back to Langfuse on the `agent-run`/`agent-turn` generation spans. | https://cloud.langfuse.com/project/cmfzimu6801jbad079856b07r/traces/arun_ticku0052pi9atgctp1ztato-trace | Attach `usage` (input/output tokens) and `model` to the `agent-run` generation spans so trace-level cost aggregates. | Our in-app agent's `agent-run` / `agent-turn` generation observations report cost=0 and null tokens/model, so we cannot monitor spend on the core agent. Fix the tracing wrapper (Bedrock + streamText path) to record `usage.input`/`usage.output` and the model name on those generation spans so Langfuse computes cost. Compare against `conversation-title` spans, which report cost correctly. |
| 3 | Slow interactive agent | Latency | **P1** | ~79 / 79 (felt by most turns) | `agent-turn` p50 **13.9s**, p90 **55s**, p99 **82s**, max **100s** (a turn hit the ceiling). For an interactive in-app copilot this is high at the median, not just the tail. Corroborated by user feedback *"worked but was slow"*. Driven by sequential tool calls (per-turn `agent-run` LLM p50 17s) and slow tools (`langfuse_listObservations` max 62s, `queryMetrics` max 31s, `createDashboardWidget` max 58s, `listScores` max 27s). | Multi-step agent loop with several slow, sequential Langfuse-API tool calls per turn; no parallelization / streaming of intermediate results. | https://cloud.langfuse.com/project/cmfzimu6801jbad079856b07r/traces/arun_twv32urios0djamn0ut8efkg-trace · https://cloud.langfuse.com/project/cmfzimu6801jbad079856b07r/traces/arun_zyppu3qhdqilir0bdwtfm7wq-trace | Parallelize independent tool calls, cap/paginate large `listObservations` pulls, and stream partial assistant text so the user sees progress before the full turn completes. | Our in-app agent's turns take p50 14s, p90 55s, up to 100s, because it makes several sequential slow Langfuse-API tool calls (`listObservations`, `queryMetrics`, `createDashboardWidget` each take tens of seconds). Reduce perceived latency: run independent tool calls concurrently, tighten default result limits/time-ranges on `listObservations`, and stream intermediate assistant messages. Example slow trace: arun_twv32urios0djamn0ut8efkg-trace (100s). |
| 4 | Empty / failed user-facing turns | Reliability | **P1** | 3 / 79 (≈3.8%) | Three `agent-turn`s delivered no useful final answer: (a) **context overflow** — *"give me last month, too"* failed with `AI_APICallError: prompt is too long: 1,371,258 tokens > 1,000,000 maximum`; (b) two turns ended on a successful tool call (`upsertDataset`, `createDashboardWidget`) with **no final assistant text** confirming the result. (Two other "empty" candidates were only the API's `<truncated due to size>` display artifact, not real empties — excluded.) | (a) unbounded context accumulation when the agent pulls a month of trace data into the prompt; (b) the agent stops after a tool call without emitting a closing summary message (compounded by tool #6 empty outputs). | https://cloud.langfuse.com/project/cmfzimu6801jbad079856b07r/traces/arun_k18etm6x5d5bedxno83tbmwg-trace · https://cloud.langfuse.com/project/cmfzimu6801jbad079856b07r/traces/arun_ng3rpcns3pr7e0iqv7z3mxjr-trace | Bound/summarize tool results before re-injecting into context (cap rows, truncate large payloads); force a final assistant summary message after terminal tool calls. | Two problems on our in-app agent: (1) a turn failed with "prompt is too long: 1.37M > 1M tokens" because it pulled a month of traces straight into context — add row caps and summarization of large tool outputs before feeding them back to the model (trace arun_k18etm6x5d5bedxno83tbmwg-trace). (2) Turns that end on a `createDashboardWidget`/`upsertDataset` tool call sometimes produce no final assistant text (trace arun_ng3rpcns3pr7e0iqv7z3mxjr-trace shows a null answer) — ensure the agent always emits a closing confirmation after a terminal tool call. |
| 5 | Malformed tool arguments | Tool/Agent | **P2** | 3 / 79 turns (5 calls) | 5 tool calls failed with top-level `{"error":true,"message":"Tool input validation failed…"}`: `langfuse_listScores` (disallowed `operator` enum value), `langfuse_getObservationFilterValues` / `langfuse_listObservations` (`root: must NOT have additional properties`). The agent recovers by retrying but wastes a round-trip and adds latency. | The agent generates filter/argument objects that don't match the tool JSON schema (invalid operator enum, extra keys). Prompt/tool-spec drift — the model's mental model of the schema is stale. | https://cloud.langfuse.com/project/cmfzimu6801jbad079856b07r/traces/arun_twv32urios0djamn0ut8efkg-trace · https://cloud.langfuse.com/project/cmfzimu6801jbad079856b07r/traces/arun_vhj5xlowigc87satggpv8u9x-trace | Tighten tool JSON schemas in the tool descriptions (enumerate allowed operators inline; mark schemas closed) and add a validation-retry hint. | Our in-app agent occasionally sends invalid arguments to Langfuse tools — e.g. `langfuse_listScores` with an `operator` not in the allowed enum, and `langfuse_getObservationFilterValues`/`langfuse_listObservations` with extra top-level properties (`root: must NOT have additional properties`). Update those tools' descriptions/schemas to inline the exact allowed operator values and the exact allowed argument keys so the model stops generating invalid calls. See trace arun_twv32urios0djamn0ut8efkg-trace. |
| 6 | Empty tool outputs (silent) | Tool/Agent | **P2** | 13 / 330 tool calls | 13 tool calls returned `null`/empty output at `DEFAULT` level (no error surfaced): `langfuse_listObservations` (7), `langfuse_queryMetrics` (5), `langfuse_createDashboardWidget` (1). At least one directly produced an empty user-facing turn (#4). Ambiguous between "no data" and silent failure — monitoring can't tell. | Some are genuine empty result sets; others (esp. on slow/62s `listObservations` calls) look like timeouts returning empty rather than erroring. `queryMetrics` returning null 5× is suspicious. | https://cloud.langfuse.com/project/cmfzimu6801jbad079856b07r/traces/arun_ahg7mxy3dhpomwsxo75mijjd-trace · https://cloud.langfuse.com/project/cmfzimu6801jbad079856b07r/traces/arun_c2l20vbuqdm09lmdztuzkvcd-trace | Make tools return an explicit `{ok:true, rowCount:0}` for genuine empties and raise `level=ERROR` on timeouts, so silent empties become distinguishable and monitorable. | Several Langfuse tool calls in our agent (`langfuse_listObservations`, `langfuse_queryMetrics`, `langfuse_createDashboardWidget`) return null/empty output at DEFAULT level with no error, so we can't tell "no matching data" from a silent timeout. Make these tools emit an explicit empty-result marker on genuine empties and set observation level ERROR on timeouts. Example: arun_ahg7mxy3dhpomwsxo75mijjd-trace (three empty listObservations on a 72s turn). |
| 7 | Opus over-provisioning | Cost | **P2** | 47 traces | `search-bar-filter` runs on **Opus-4.8** for all 32 calls — a small NL→filter-JSON task — costing **$1.32** (≈87% of all *captured* LLM cost) at $0.041/call. Additionally 15/117 `conversation-title` generations run on Opus vs the Haiku baseline used for the other 102. Both are cheap, structured tasks that don't need the flagship model. | Default model for the filter feature (and a fraction of title-gen) is set to Opus; no model-tiering by task complexity. | https://cloud.langfuse.com/project/cmfzimu6801jbad079856b07r/traces/b169158d1e83d90534d210ae71acfa82 | Move `search-bar-filter` and all `conversation-title` generation to Haiku (or Sonnet if quality dips); A/B on filter-JSON validity before/after. | Our `search-bar-filter` feature (NL→Langfuse-filter-JSON) runs on claude-opus-4-8 at ~$0.04/call, and 15 of 117 conversation-title generations also use Opus while the rest use Haiku. These are small structured-output tasks. Switch both to Haiku-4.5, and validate that filter-JSON output validity stays ≥ current (currently 32/33 parse cleanly) before rolling out. |
| 8 | Conv-root spans: null I/O + runaway duration | Coverage | **P3** | 25 / 25 | `in-app-agent` conversation-root spans carry `input=output=null` and session durations up to **5943s (99 min)** — this is *session duration*, not per-turn latency, but such long-lived roots suggest never-closed spans and add no per-turn I/O signal. | Conversation-level root span stays open across the session and doesn't capture I/O (that lives on child `agent-turn`); no explicit close on session end. | https://cloud.langfuse.com/project/cmfzimu6801jbad079856b07r/traces/aconv_stmao6fs831q4l2v95mq51tk | Close conversation-root spans on session end (or set a max), and note that these should not be used for latency dashboards. | Our `in-app-agent` conversation-root spans have null I/O and stay open up to ~99 minutes, inflating any latency chart built at the trace root. Ensure these roots are closed on session end and exclude them from per-turn latency metrics (use `agent-turn` for latency instead). |

---

## Verified non-issues (checked, held up)

- **Language matching (G/H30):** clean. 15/79 agent-turns are Italian; the agent consistently replies in Italian to Italian users (verified across an 11-turn IT session, `aconv_eezdndyapxhrbllb8ubktqzp`).
- **Refusals / non-answers (G24):** none found on in-scope traffic.
- **Out-of-scope handling (H28):** all sampled inputs are in-scope (users analyzing their own Langfuse data — traces, evals, dashboards, datasets, LLMAJ calibration). No off-topic over-serving observed.
- **Negative user feedback (E16):** none — both scores are positive thumbs-up.
- **Model truncation (A4):** no `finish_reason=length` truncations observed (the two `<truncated due to size>` cases are Langfuse storage-display limits, not model cutoffs).
- **Structured-output validity (G25):** `search-bar-filter`/`natural-language-filter` output is 32/33 valid JSON; the 1 failure is the single largest/costliest filter (1500+ chars, likely storage-truncated) — logged as a borderline note, not a finding.
- **Input distribution (H/J):** not synthetic-skewed (65 distinct inputs / 79 turns, 41 users) — aggregate rates are trustworthy.
- **User/session concentration (I32):** issues are spread across users; no single user drives the findings (top user 33/254).

## Suggested order of action
1. **#1 + #2 (Coverage):** stand up a production quality evaluator and fix core-agent cost/token instrumentation — you are currently flying blind on both quality and spend for the main product.
2. **#3 + #4 (Latency/Reliability):** parallelize tools + bound context; these are the felt user pain and the source of the empty/failed turns.
3. **#5 + #6 (Tool robustness):** tighten tool schemas and make empty/timeout returns explicit.
4. **#7 (Cost):** downgrade filter + title generation off Opus — quick, safe saving.
5. **#8 (Hygiene):** close conversation-root spans.

## Open questions
- Are the empty `queryMetrics`/`listObservations` returns genuine "no data" or silent timeouts? (Needs tool-side logging — #6.)
- Is `agent-turn` latency dominated by tool wall-time or by the number of sequential LLM steps? (Cost/token instrumentation from #2 would let us decompose it.)
- Is the repeated-question friction (one IT session re-asked the same widget question 4×) client-side re-send or genuine user re-asking?

## Not covered
- **No LLM-judge scores exist**, so score distributions, polarity analysis, dead-evaluator checks, and score regressions (E16–E19) could not be computed — this absence *is* finding #1.
- **agent-turn cost/tokens un-instrumented** (#2), so true spend and context-growth on the core agent could not be measured.
- **Faithfulness / hallucination (G26)** not assessed — no ground truth, and some tool outputs are redacted.
- **Two large agent-turn outputs** were `<truncated due to size>` in trace storage and could not be read in full.
- **Retrieval relevance (D14)** only lightly sampled (`langfuseDocs_searchLangfuseDocs`, 11 calls) — returned relevant Langfuse doc URLs on inspection, but not exhaustively scored.
- Analysis is the **full window (254 traces, no sampling cap hit)**; observation cursor paginated cleanly to completion (9 pages, 865 obs).
