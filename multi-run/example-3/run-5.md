# Langfuse Issue-Detection Triage — run #5

**Project:** `cmfzimu6801jbad079856b07r` (langfuse-in-app-agent) · **Host:** https://cloud.langfuse.com (EU cloud)
**Generated:** 2026-07-08 (UTC)

## Overall stats

### Method & scope
- **Window analyzed:** `2026-07-01T19:08:51Z` → `2026-07-08T19:08:51Z` (last 7 days, UTC).
- **Environments included (product):** `langfuse-in-app-agent` (221 traces) — the in-app Langfuse assistant. Three trace names:
  - `agent-turn` (79) — the **core user-facing agent turn** (MCP-tool agent that queries the user's Langfuse observability data). This is the primary product.
  - `in-app-agent-conversation-title` (117) — a lightweight LLM utility that titles conversations.
  - `in-app-agent` (25) — **session-root container spans** (id == sessionId; null I/O).
- **Secondary environment examined:** `langfuse-natural-language-filter` (33 traces) — `search-bar-filter` (32) + `natural-language-filter` (1), a natural-language→filter-JSON generator. Reported below but not the primary "agent" product.
- **Excluded:** none for machinery reasons — there were **no** evaluator (`langfuse-llm-as-a-judge`) executions, dataset runs, or playground traces in the window (0 excluded).
- **Volume examined:** **254 traces** (all, 3 pages), **865 observations** (all; 330 TOOL, 281 GENERATION, 254 SPAN — cursor-paginated in full), **330 TOOL observations pulled with I/O content**, **147 scores** pulled (only **2** fall inside the window).

### Input distribution (trust check — done before any rate)
`agent-turn`: 65 distinct user inputs across 79 turns, 25 distinct users, 34 sessions. **No synthetic/canned input dominates** (top input appears 4×). Traffic is genuine, diverse, human product usage — so the rates below are **not** skewed by demo/CI traffic. This is a verified-clean signal.

### Headline metrics (core product = `agent-turn`, n=79)
| Metric | Value |
|---|---|
| Turns / users / sessions | 79 / 25 / 34 |
| End-to-end latency (root) | p50 **13.9s**, p90 **58.0s**, p99/max **~100.0s** (ceiling) |
| GENERATION latency | p50 2.8s, p90 31.7s, p99 77.3s, max 100.0s · TTFT p50 2.8s / p90 5.9s |
| TOOL latency | p50 ~0s, p90 6.0s, p99 **52.9s**, max 62.4s |
| Cost | total (all obs) **$1.52**; per-trace mean $0.006, max $0.063 — **low** |
| Tokens/turn | p50 ~800, p90 ~7k, typical max ~9.5k; **one outlier 1.37M (crashed)** |
| Hard errors | 6 ERROR obs (5 tool-arg validation, 1 generation context-overflow) |
| **Score coverage in-window** | **~0%** — 2 feedback scores / 79 turns; **0 evaluator scores** |

## Findings

| # | Issue | Category | Priority | Traces affected | Description | Root-cause hypothesis | Example traces | Proposed fix | Agent prompt |
|---|---|---|---|---|---|---|---|---|---|
| 1 | Quality evaluators dormant — zero eval coverage on live traffic | Scores/Evals | **P0** | 79 / 79 turns unevaluated | The two LLM-judge evaluators (`in-app-agent-usable-answer`, `in-app-agent-mcp-incorrect-call`) last produced scores on **2026-06-26**; **0 scores in the 7-day window**. The only in-window scores are **2** `in_app_agent_feedback` annotations (both positive). So the product's **primary quality metric produces no signal on 100% of current traffic** and nothing else measures it — the team cannot see the agent degrade. NOTE: the evaluators themselves are **valid** — I read their comments: they clearly reference the assistant's actual output and show a healthy value spread (usable-answer 0.0–1.0; mcp-incorrect-call 66×False/5×True). This is a **not-running / coverage** problem, not a wiring bug. | Evaluator run rules disabled, unscheduled, or a batch backfill on 06-26 that was never re-enabled; possibly sampling that no longer targets `in-app-agent` conversation traces. | https://cloud.langfuse.com/project/cmfzimu6801jbad079856b07r/traces/aconv_mqjveujsqgl3ds5geq522sxc (last-scored 06-26) ; current unscored: https://cloud.langfuse.com/project/cmfzimu6801jbad079856b07r/traces/arun_f0mwsa9a66y2acarvqrjuo4m-trace | Re-enable / re-schedule both evaluation rules so they fire on new `in-app-agent` conversation traces; add an alert when daily eval volume drops to 0; run a known-positive sanity check to confirm they fire. | The two Langfuse evaluators `in-app-agent-usable-answer` and `in-app-agent-mcp-incorrect-call` stopped producing scores after 2026-06-26 — our last 7 days of agent traffic (79 `agent-turn` / `in-app-agent` conversation traces) have zero automated quality scores. The evaluators are correctly wired (their reasoning comments reference the real assistant output). Investigate why the evaluation rules stopped firing: check the rule's enabled state, schedule, target filter (they score `aconv_*` conversation traces), and sampling rate in the project's Evaluators config. Restore continuous scoring and add a monitor that alerts if eval throughput hits 0 for >24h. |
| 2 | Context overflow crashes turns; oversized outputs | Reliability | **P1** | 2 / 79 (2.5%) | 2 turns returned `"<truncated due to size exceeding limit>"` as output. One (`arun_k18etm…`) accumulated **1,371,258 tokens > 1,000,000 model max** → `AI_APICallError: prompt is too long`, the whole turn failed and the user (asking the follow-up "give me last month, too") got broken output. The other (`arun_k6kjd87…`, "create 1 dataset based on production data") also blew past the display/size limit. Both belong to the **same power user** running heavy production-data analysis. | Unbounded accumulation of large tool results (full observation/trace payloads) into the conversation context across multi-step turns; no context truncation/summarization or per-tool result size cap. | https://cloud.langfuse.com/project/cmfzimu6801jbad079856b07r/traces/arun_k18etm6x5d5bedxno83tbmwg-trace ; https://cloud.langfuse.com/project/cmfzimu6801jbad079856b07r/traces/arun_k6kjd87uunn29gwt43yqp7t5-trace | Cap/summarize large tool results before re-injecting into context; paginate or head-truncate `listObservations`/`getObservation` payloads; detect approaching context limit and compact history; return a graceful "result too large, narrowing…" instead of crashing. | An `agent-turn` failed with `prompt is too long: 1371258 tokens > 1000000 maximum` (trace `arun_k18etm6x5d5bedxno83tbmwg-trace`, user follow-up "give me last month, too"). The agent stuffs full tool results (large `langfuse_listObservations`/`getObservation` payloads) into context every step with no size cap, so on data-heavy multi-turn sessions the prompt exceeds the model's 1M limit and the turn dies. Add a per-tool-result size limit + summarization/compaction of prior tool outputs, and a pre-call token budget check that trims history rather than erroring. Verify by replaying this session. |
| 3 | High interactive latency | Latency | **P1** | ~majority of turns | For an interactive in-app assistant, `agent-turn` p50 = **13.9s**, p90 = **58s**, p99 ≈ **100s** (a ceiling suggesting a ~100s timeout). Driven by slow tool calls (TOOL p99 **52.9s**, max 62s) and multi-step sequential LLM+tool chains. Corroborated by user feedback: value=1 but comment **"nice worked but was slow."** | Sequential multi-step tool/LLM loops with individually slow MCP data queries (large ClickHouse-backed observation queries); no parallelization; slow queries not time-boxed. | https://cloud.langfuse.com/project/cmfzimu6801jbad079856b07r/traces/arun_duuwvm2q0aftd6cjb7ps2vv7-trace | Time-box slow tool queries, parallelize independent tool calls, stream partial progress to the user, and profile the p99 TOOL calls (`listObservations`, `queryMetrics`) for query optimization. | Our in-app agent's turns are slow: end-to-end p50 ≈ 14s, p90 ≈ 58s, and the slowest single tool calls (`langfuse_listObservations`, `langfuse_queryMetrics`) run 50–62s. A user explicitly said "worked but was slow." Investigate the slowest TOOL observations, add per-tool timeouts, run independent tool calls in parallel, and stream intermediate status so the interactive experience doesn't feel stalled. Trace `arun_duuwvm2q0aftd6cjb7ps2vv7-trace` is a good starting example. |
| 4 | Malformed tool arguments (incl. `&lt;` HTML-escape leak) | Tool/Agent | **P2** | 3 / 79 (5 calls / 330 = 1.5%) | 5 TOOL calls failed **input-validation** at ERROR level. Two `langfuse_listScores` calls sent `operator: "&lt;"` — an **HTML-escaped `<`** leaked into the argument value, rejected as not-an-allowed-operator. Three others (`getObservationFilterValues`, `listObservations`) failed `must NOT have additional properties` — the model passed unsupported params (`environment`, `observationType`, `hasParentObservation`, `traceName`). Agent recovered by retrying, wasting turns/latency. `&quot;` appears pervasively in stored tool I/O, hinting the tool-call serialization HTML-escapes content. | Tool-arg rendering/serialization HTML-escapes operator characters (`<`→`&lt;`); and the tool JSON-schemas the model sees allow params the API rejects (schema/doc drift between the model's tool spec and the actual endpoint). | https://cloud.langfuse.com/project/cmfzimu6801jbad079856b07r/traces/arun_twv32urios0djamn0ut8efkg-trace ; https://cloud.langfuse.com/project/cmfzimu6801jbad079856b07r/traces/aconv_qof1l210s79a4uzl1oljacro | Stop HTML-escaping tool-argument values before dispatch (decode entities or don't encode); tighten tool JSON-schemas so the model can't emit unsupported params (or accept+ignore them). | Some agent tool calls fail validation. `langfuse_listScores` was called with `operator: "&lt;"` (an HTML-escaped `<`) and got rejected — something in the tool-call arg pipeline is HTML-entity-encoding operator characters. Separately, `langfuse_getObservationFilterValues`/`langfuse_listObservations` were rejected for "additional properties" (`environment`, `observationType`, `hasParentObservation`). Fix the serialization so `<`/`>` aren't escaped in arg values, and reconcile the tool schemas exposed to the model with the actual API so unsupported fields can't be emitted. See trace `arun_twv32urios0djamn0ut8efkg-trace`. |
| 5 | Filter-generator contract violation (prose after JSON) | Output quality | **P2** | 1 / 33 filter-gen (3%) | `search-bar-filter` is contracted to output **only** a JSON filter array, but one output appended prose after the JSON ("Wait — traceName is a stringOptions (enum) column and cannot express 'starts with'…"), making the output unparseable by the downstream filter consumer. Also format inconsistency across the feature: `search-bar-filter` emits a bare array while `natural-language-filter` emits `{"filters":[…]}`. | Model "thinks out loud" past the JSON when the request is hard to satisfy (no valid operator), instead of returning valid JSON + a comment field; prompt doesn't hard-stop after the JSON. | https://cloud.langfuse.com/project/cmfzimu6801jbad079856b07r/traces/b169158d1e83d90534d210ae71acfa82 | Enforce JSON-only output (response_format / stop sequence); if the request can't be satisfied, return a valid empty/closest-match array plus reasoning in a separate field, never trailing prose. Standardize the output schema across both filter features. | The `search-bar-filter` LLM must return ONLY a JSON filter array, but on the request "trace name starts with EA-Reviewer-Invitations" it appended an explanatory sentence after the JSON, breaking the parser (trace `b169158d1e83d90534d210ae71acfa82`). Enforce JSON-only output and route any caveat into a structured field instead of trailing prose. Also align the output schema: `search-bar-filter` returns a bare array while `natural-language-filter` wraps it in `{"filters":[…]}`. |
| 6 | Empty tool-query results / query reformulation | Tool/Agent | **P3** | 13 / 330 tool calls (4%) | 13 tool calls (`queryMetrics`, `listObservations`) returned empty result sets, occasionally causing the agent to reformulate. No hard failure; mild latency/token waste. `ok:false` count = **0** (no silent tool failures). | Agent constructs queries with filters/time-ranges that match no rows, then retries with different params. | https://cloud.langfuse.com/project/cmfzimu6801jbad079856b07r/traces/arun_ahg7mxy3dhpomwsxo75mijjd-trace | Have tools return a hint (e.g. available filter values / nearest non-empty range) on empty results so the agent self-corrects in one step. | Low priority: ~4% of agent tool calls return empty result sets (mostly `queryMetrics`/`listObservations`) and the agent then reformulates, wasting a step. Consider returning guidance (valid filter values, nearest populated time range) on empty results to reduce round-trips. |
| 7 | Monitoring coverage gaps: version/release/tags | Coverage | **P3** | 254 / 254 | `release` and `version` are **null on every trace** → no release/version regression analysis is possible. Tags present on only 104/254 traces. | Instrumentation doesn't set `release`/`version`; tagging inconsistent. | https://cloud.langfuse.com/project/cmfzimu6801jbad079856b07r/traces/arun_f0mwsa9a66y2acarvqrjuo4m-trace | Set `release`/`version` (git SHA / app version) on every trace so deploys can be correlated with quality/latency/cost regressions. | Our Langfuse traces never set `release` or `version`, so we can't detect deploy-related regressions. Add `release` (git SHA) and `version` (app version) to the tracing context for the in-app agent, and tag traces consistently. |
| 8 | Session-root spans: null I/O, session-duration mislabeled as latency | Coverage | **P3** | 25 / 25 `in-app-agent` | All session-root `in-app-agent` traces have **null input/output** and huge "latency" (p90 ~2667s, p99 ~5942s ≈ 99 min). These are open session-container spans (session duration), **not** per-turn latency — easy to misread in dashboards. | Session/conversation root span left open for the session lifetime; no I/O attached at that level. | https://cloud.langfuse.com/project/cmfzimu6801jbad079856b07r/traces/aconv_stmao6fs831q4l2v95mq51tk | Label session spans distinctly and exclude them from per-turn latency dashboards; or attach a summary I/O. Measure turn latency at `agent-turn`. | Our `in-app-agent` session-root traces have null I/O and multi-thousand-second durations because they wrap the whole session. Ensure latency dashboards measure `agent-turn` (per-turn) not the session span, and label session spans so they aren't counted as turn latency. |
| 9 | Conversation-title length spec drift | Output quality | **P3** | 11 / 117 (9%) | `in-app-agent-conversation-title` is prompted for "3–6 words" but 11 titles are 7–8 words. All well-formed and readable; purely a spec-adherence nit. | Prompt rule not strictly enforced. | https://cloud.langfuse.com/project/cmfzimu6801jbad079856b07r/traces/aa0896c4a0d1dfdd711f91b32dc26722 | Minor: tighten prompt or post-trim to 6 words if the limit matters. | Low priority: the conversation-title generator occasionally emits 7–8 words vs the "3–6 words" rule. Tighten the prompt or truncate if strict length matters. |

## Verified non-issues (checked, held up)
- **Input distribution / synthetic skew** — clean: 65 distinct inputs / 79 turns, 25 users; no canned input dominates. Rates are trustworthy.
- **Cost & tokens** — clean: total ~$1.52, max $0.063/trace, typical <10k tokens/turn. Only issue is the single 1.37M-token overflow (Finding #2).
- **Silent tool failures (`ok:false`)** — none (0/330). The many "error"-word matches in tool outputs are the agent legitimately querying *data about* errors (error-analysis docs, `mcp-incorrect-call` evaluator name, observation listings), not tool failures.
- **Evaluator validity** — verified: comments show both evaluators read the real assistant output and discriminate well. NOT mis-wired (the problem is dormancy, Finding #1).
- **Language handling** — clean: Italian-language users receive fluent Italian responses (9 turns checked); no language mismatch.
- **Safety / PII** — clean: no genuine secret leakage. `pk-lf-*` are public keys (non-sensitive); `sk-lf-123456` is a doc placeholder; "password"/"secret_key" strings appear only inside surfaced Langfuse setup docs/examples.
- **Refusals** — 3 refusal-ish phrasings, all legitimate ("Langfuse doesn't support X" feature answers), not over-refusal.
- **Retrieval relevance** — this agent uses structured MCP data-query tools plus `langfuseDocs` search; doc searches returned on-topic pages. No broken-retrieval (stale/same-doc-for-any-query) pattern.
- **Tool loops** — one trace called `langfuse_listScores` 24× (heavy but legitimate exploration that resolved); no infinite/no-progress loops.
- **Out-of-scope / abandonment / frustration** — all inputs on-domain (Langfuse tasks); friction-word matches (2) were analytical phrasing, not complaints.
- **Filter-gen output validity** — 32/33 produced valid filter JSON (1 exception = Finding #5).

## Suggested order of action
1. **Finding #1 (P0)** — restore evaluator scoring + add a zero-throughput alert. Without it, everything else is unmeasured.
2. **Finding #2 (P1)** — cap/compact tool-result context to stop the 1M-token crashes.
3. **Finding #3 (P1)** — attack latency (tool timeouts, parallelism, streaming).
4. **Findings #4–5 (P2)** — fix arg HTML-escaping / schema drift and the filter-gen JSON contract.
5. **Findings #6–9 (P3)** — coverage/hygiene.

## Open questions
- Are the two evaluators *meant* to run continuously (real-time) or as periodic batch? If batch, Finding #1's severity depends on the intended cadence — confirm expected behavior.
- Is the ~100s latency ceiling a deliberate turn timeout? If so, turns hitting it are silently cut short.
- Should the heavy-data power-user workload (Finding #2) be routed to a paginated/streamed path?

## Not covered
- **Version/release segmentation** — impossible (all null; Finding #7).
- **In-window score distributions** — no evaluator scores exist inside the window (Finding #1), so distribution/regression analysis of eval scores is limited to the pre-window 06-26 batch.
- **Faithfulness/hallucination** — no ground truth; spot-checked outputs referenced real queried data and looked grounded, but not systematically verified.
- **Full transcript read** — sampled conversations end-to-end (multi-turn, no-final-answer, Italian, feedback-scored) rather than all 79.
- **Session-root latency** — reported as session duration, not investigated as a bug (Finding #8).
